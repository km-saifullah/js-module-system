let piValue = Math.PI;

export let addition = (num1, num2) => {
  return num1 + num2;
};

export let subtruction = (num1, num2) => {
  return num1 - num2;
};

export let product = (num1, num2) => {
  return num1 * num2;
};

export let division = (num1, num2) => {
  return num1 / num2;
};

export default piValue;
